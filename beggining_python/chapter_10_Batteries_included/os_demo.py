import os

# print(os.environ) mapping with environment variables

# os.system("cd ..") executes the command in subshell

print(os.sep, "Separator used in paths")

print(os.pathsep, "Separator to separate paths")

print(os.linesep, "Line separator")

print(os.urandom(8), "8 bytes of cryptographycally strong random data")